/**
 * Valluvam API — Root Entry Point.
 * Forwards to src/server.js to ensure consistent execution regardless
 * of whether the server is started with `npm start` or `node index.js`.
 */
require('./src/server');


